// lib/core/utils/web_geolocation.dart
//
// STUB mobile/desktop — remplace dart:html par une implémentation vide.
// Sur Android/iOS, la géolocalisation passe par geolocator (pas dart:html).

Future<({double latitude, double longitude})?> getWebGeolocation() async {
  // Non disponible sur mobile — retourne null
  return null;
}